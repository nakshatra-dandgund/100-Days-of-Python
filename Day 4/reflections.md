# 💭 Reflection - Day 4

### 🧠 What I Learned
- How Python treats numbers and strings differently.  
- That `"2" + "3"` results in `"23"`, but `int("2") + int("3")` gives `5`.  
- The importance of typecasting before performing calculations.  

### ⚡ What Went Wrong
- Initially tried adding two strings and got unexpected results.
- Realized that Python doesn’t automatically convert strings to numbers — it must be done manually.

### 💪 My Feelings
This day was short but powerful!  
I finally understood **why inputs from `input()` are always strings** and how to convert them properly.  

### 🌱 Next Steps
- Practice converting between `int`, `float`, and `str`.  
- Try combining user input with typecasting.  
- Build a small program that takes numeric input from users and performs calculations.
